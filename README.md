# atlas-app

to start mongodb 
```bash
docker-compose up -d
```
install the dependencies
```bash
npm install
```

then start the server
```bash
node server.js
```

finally start the client
```bash
cd todoapp
npm install
npm start
```

